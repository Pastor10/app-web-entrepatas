import {Marquesina} from './marquesina.model';
export class ListaMarquesinaMarquesina {
  numOrden: number;
  marquesina: Marquesina;

  constructor(numOrden, marquesina) {
    this.numOrden = numOrden;
    this.marquesina = marquesina;
  }
}
