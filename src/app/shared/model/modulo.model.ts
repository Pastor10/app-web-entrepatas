export class Modulo {
  ruta: string;
  title: string;
  icon: string;
  orden: number;

  constructor(icon,ruta, title,orden) {
    this.icon=icon;
    this.ruta = ruta;
    this.title = title;
    this.orden=orden;
  }

}
